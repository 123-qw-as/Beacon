# 章节撰写：模型评价与结论

本轮你**只**撰写一个字段：`conclusion`。其余章节由其他轮次处理，不要输出。

{% include 'writer_iron_rules.md.j2' %}

---

## 本段大纲锚点
{{ outline_conclusion }}

---

## 题目
{{ problem }}

## 模型演化（概览）
{% for m in model_versions %}
- [{{ m.stage }}] {{ m.description[:80] }}
{% endfor %}

## 代码运行结果（关键结果摘要）
{% for a in code_artifacts %}
- [{{ loop.index }}] {{ a.purpose }} — success={{ a.success }}
```
{{ a.stdout[-200:] }}
```
{% endfor %}

## 敏感性分析结果
{% if sensitivity_runs %}
{% for r in sensitivity_runs %}
- **参数 `{{ r.parameter }}`**：指标 `{{ r.metric }}` = {{ r.results }}（{{ r.interpretation or "无解读" }}）
{% endfor %}
{% else %}
（暂无敏感性结果）
{% endif %}

---

{% if prior_critic %}
## 上一轮 PaperCritic 反馈（必须逐条回应）

PaperCritic 评分：{{ prior_critic.score }}/10，approved={{ prior_critic.approved }}。

**issues**（仅与本段相关，其余可忽略）：
{% for i in prior_critic.issues %}
- [{{ i.section }}] {{ i.problem }}
{% endfor %}

本次重写时**必须**逐条回应上述与本段相关的 issues；不能简单复述上一稿。
{% endif %}

---

## 字段字数预算与结构要求

| 字段 | 字数区间 | 强制结构 |
|---|---|---|
| `conclusion` | 300–500 | 三段式：优点 / 缺点 / 推广方向 |

结论段允许一次「综上」开头，但禁用「众所周知」「显而易见」等空泛开头。每个子论点独占一段，段间空行。

{% if retrieved_context %}

---

## 检索到的参考资料（仅供启发，不可照抄）
{{ retrieved_context }}
{% endif %}

---

## 输出契约

请返回严格符合下列 schema 的 JSON（**不得包含其他键**）：

```json
{
  "conclusion": str
}
```
