# 章节撰写：模型的求解

本轮你**只**撰写一个字段：`solution`。其余章节由其他轮次处理，不要输出。

{% include 'writer_iron_rules.md.j2' %}

---

## 本段大纲锚点
{{ outline_solution }}

---

## 题目
{{ problem }}

## 模型方程（用于求解引用）
{% for m in model_versions %}
{% for eq in m.equations %}
- \[ {{ eq }} \]
{% endfor %}
{% endfor %}

## 代码运行结果（按 artifact 分组，每段保留末尾 500 字符以保住最终数字）
{% for a in code_artifacts %}
### [{{ loop.index }}] {{ a.purpose }} — success={{ a.success }}
```
{{ a.stdout[-500:] }}
```
{% if a.stderr and not a.success %}**stderr**：`{{ a.stderr[-300:] }}`{% endif %}
{% endfor %}

## 正式主方案代码（算法名称与步骤以此为准）
{% for a in code_artifacts %}
{% if a.evidence_role == "primary" %}
```python
{{ a.code }}
```
{% endif %}
{% endfor %}

---

{% if prior_critic %}
## 上一轮 PaperCritic 反馈（必须逐条回应）

PaperCritic 评分：{{ prior_critic.score }}/10，approved={{ prior_critic.approved }}。

**issues**（仅与本段相关，其余可忽略）：
{% for i in prior_critic.issues %}
- [{{ i.section }}] {{ i.problem }}
{% endfor %}

本次重写时**必须**逐条回应上述与本段相关的 issues；不能简单复述上一稿。
{% endif %}

---

## 字段字数预算与结构要求

| 字段 | 字数区间 | 强制结构 |
|---|---|---|
| `solution` | 2800–4000 | 必须按以下内容组织：算法流程、复杂度与停止条件、实验设计、逐问题结果、基线比较、动态调整和证据边界，用 `##` 标题分隔。 |

### 两段结构（用 `##` 标题）

**## 求解算法与流程**
- 算法名称必须写成“容量拆分与逐步最小增量构造启发式”；禁止写遗传算法、ALNS、动态规划或精确求解器已经执行
- 算法描述与复杂度分析（用文字描述流程，不输出代码块）
- 关键步骤的公式代入（把通用方程填入题目具体参数）
- 收敛性/最优性保证（若有）

**## 实验设计与对比分析**
- 实验环境（数据规模 / 时间窗口）
- 主方案加至少 2 个有效基线的描述与设置理由；本次有效基线仅为 no_schedule 与 greedy
- 各方案定量结果（从上方「代码运行结果」提取，必须与 stdout 一致）
- 结果分析：每个结论用「结论→依据→分析」结构，依据来自代码 stdout

**IRON RULE 1（禁编造）铁律**：solution 中的所有数字必须可在上方「代码运行结果」中追溯。无来源的数字一律改为定性表述。

**单位口径**：`avg_delivery_time=549.73` 由源码中的分钟制时间递推得到，单位为分钟；`response_time=0.004103` 由 `time.time()` 差值得到，单位为秒。`vehicles=604` 表示车辆/路线数，不是客户点数或配送点数。

{% if retrieved_context %}

---

## 检索到的参考资料（仅供启发，不可照抄）
{{ retrieved_context }}
{% endif %}

---

## 输出契约

请返回严格符合下列 schema 的 JSON（**不得包含其他键**）：

```json
{
  "solution": str
}
```
