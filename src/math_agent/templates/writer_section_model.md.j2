# 章节撰写：模型的建立与演化

本轮你**只**撰写一个字段：`model_section`。其余章节由其他轮次处理，不要输出。

{% include 'writer_iron_rules.md.j2' %}

---

## 本段大纲锚点
{{ outline_model_section }}

---

## 题目
{{ problem }}

## 已确认假设（{{ assumptions | length }} 条）
{% for a in assumptions %}
- {{ loop.index }}. {{ a.statement }}
{% endfor %}

## 模型演化（含推导链；每字段已截断至 100 字符）
{% for m in model_versions %}
### {{ loop.index }}. [{{ m.stage }}] {{ m.description[:100] }}
{{ m.description[:100] }}

**方程**：
{% for eq in m.equations %}
- \[ {{ eq }} \]
{% endfor %}

**变量**：
{% for k, v in m.variables.items() %}
- `{{ k }}`：{{ v }}
{% endfor %}

{% if m.derivation_steps %}
**推导链**：
{% for d in m.derivation_steps %}
- **{{ d.title[:100] }}**
  - 动机：{{ d.motivation[:100] }}
  - 陈述：{{ d.statement[:100] }}
  - 结果：{{ d.result[:100] }}
{% endfor %}
{% endif %}

{% if m.notes %}**与上版差异**：{{ m.notes[:100] }}{% endif %}
{% endfor %}

---

{% if prior_critic %}
## 上一轮 PaperCritic 反馈（必须逐条回应）

PaperCritic 评分：{{ prior_critic.score }}/10，approved={{ prior_critic.approved }}。

**issues**（仅与本段相关，其余可忽略）：
{% for i in prior_critic.issues %}
- [{{ i.section }}] {{ i.problem }}
{% endfor %}

本次重写时**必须**逐条回应上述与本段相关的 issues；不能简单复述上一稿。
{% endif %}

---

## 正式主方案代码（模型叙述必须与此实现一致）
{% for a in code_artifacts %}
{% if a.evidence_role == "primary" %}
```python
{{ a.code }}
```
{% endif %}
{% endfor %}

## 字段字数预算与结构要求

| 字段 | 字数区间 | 强制结构 |
|---|---|---|
| `model_section` | 4500–6500 | 必须按以下三段组织，每段 ≥1200 字，并补充数据到变量的映射、参数口径、成本分解与验证层次，用 `##` 标题分隔。以 final 模型方程为骨架，并以正式主方案代码纠正不一致表述。 |

### 三段结构（用 `##` 标题）

**## 静态绿色配送优化模型**
- 完整写出五项成本目标函数，不得只写速度或能耗子式
- 定义路径、车辆启用、车型、到达/服务时间、载重与容积变量
- 按客户覆盖、流守恒、配送中心起讫、载重、容积、车辆上限、软时间窗和时变行驶时间逐组给出约束
- 本题没有经代码验证的机器学习预测器；禁止编造 XGBoost、STGNN、遗传算法、ALNS 或动态规划

**## 绿色配送区政策扩展**
- 附件坐标字段为 `X (km), Y (km)`，必须使用欧氏距离；禁止写 Haversine 经纬度距离
- 说明半径10 km绿色区、8:00–16:00燃油车禁入、线段与圆边界相交判定
- 区分硬约束、软时间窗惩罚与评价指标，说明求解实现中的逐候选可行性检查
- 明确该模型含速度依赖非线性能耗，程序采用构造式启发式求可行解，不宣称精确最优性

**## 动态事件与局部调整**
- 只描述正式代码实际执行的订单取消后局部重新插入场景
- 给出受影响订单删除、遍历其他路线插入位置、载重/容积/每车客户数可行性检查、最小新增距离选择
- `response_time` 是 Python `time.time()` 的秒数；不得改写成分钟或小时
- 不得声称代码未实现的滚动时域、CVaR、场景树或四类事件均已数值验证

每段首句点明该段相对于更简单方案的增量价值。每个子论点独占一段，段间空行。若题目无调度需求，将"优化调度"段改为"模型优化与求解策略"；若无不确定性需求，将"动态扩展"段改为"模型泛化性讨论"。

{% if retrieved_context %}

---

## 检索到的参考资料（仅供启发，不可照抄）
{{ retrieved_context }}
{% endif %}

---

## 输出契约

请返回严格符合下列 schema 的 JSON（**不得包含其他键**）：

```json
{
  "model_section": str
}
```
