# Beacon frontend

Static frontend prototype for the math-agent project. It uses the Beacon logo and presents a practical math modeling pipeline workspace.

## Preview

Open `frontend/index.html` directly, or run the bundled static server from the repository root:

```bash
node frontend/server.mjs
```

Then visit `http://127.0.0.1:5173`.
